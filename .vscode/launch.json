{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/žlang",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}